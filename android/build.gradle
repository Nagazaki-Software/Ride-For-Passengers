allprojects {
    repositories {
        google()
        mavenCentral()
        // CardinalCommerce (3DS dependency)
        maven { url 'https://cardinalcommerce.jfrog.io/artifactory/android' }
        maven { url 'https://cardinalcommerceprod.jfrog.io/artifactory/android' }
    }
}

rootProject.buildDir = '../build'

subprojects { sub ->
    afterEvaluate {
        def hasAndroid = sub.extensions.findByName("android")
        if (hasAndroid != null) {
            // Funciona tanto para com.android.application quanto com.android.library
            sub.android.compileSdkVersion 35
            // (opcional, mas ajuda)
            sub.android.defaultConfig {
                if (it.hasProperty('targetSdkVersion')) targetSdkVersion 35
                if (it.hasProperty('minSdkVersion'))    minSdkVersion 23
            }
            // Namespace pra plugins antigos que não definem
            if (!sub.android.hasProperty('namespace') || sub.android.namespace == null) {
                sub.android.namespace = "${sub.group}.${sub.name}".replace('-', '_')
            }
        }
    }
}

<<<<<<< HEAD
<<<<<<< HEAD
=======
=======
>>>>>>> 10c9b5c9503d954411773ec70615ce97229cb3be
// Map CardinalCommerce artifact to a mirror if the official repo is unavailable
subprojects { sub ->
    configurations.all {
        resolutionStrategy.eachDependency { DependencyResolveDetails details ->
            if (details.requested.group == 'org.jfrog.cardinalcommerce.gradle' &&
                details.requested.name == 'cardinalmobilesdk') {
                // Use a compatible mirror available on Maven Central
                details.useTarget 'io.github.airwallex:cardinalmobilesdk:2.2.3-2'
            }
        }
    }
}

<<<<<<< HEAD
>>>>>>> 10c9b5c (new frkdfm)
=======
>>>>>>> 10c9b5c9503d954411773ec70615ce97229cb3be
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) { delete rootProject.buildDir }
