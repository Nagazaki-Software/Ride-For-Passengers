<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema usado depois do splash -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Tema de lançamento (pré-Android 12) -->
    <style name="LaunchTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Alias para evitar o erro se algo ainda referir Theme.SplashScreen -->
    <style name="Theme.SplashScreen" parent="@style/LaunchTheme"/>
    <style name="Theme.MyApp" parent="@style/NormalTheme"/>

</resources>
